apiVersion: v1
kind: Service
metadata:
  name: api-crawler
  namespace: crawler
spec:
  selector:
    app: synergychat-crawler
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080

